[phases.setup]
nixPkgs = ["ffmpeg", "python311", "nodejs_20", "npm-9_x"]

[phases.install]
cmds = [
  "pip install -r requirements.txt --no-cache-dir",
  "npm install"
]

[start]
cmd = "python main.py"
